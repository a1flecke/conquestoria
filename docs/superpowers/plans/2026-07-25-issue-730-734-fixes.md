# Issues 730–734 Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminate the five reported desktop UI, unit rendering, visual identity, stale-warning, and zero-movement attack regressions.

**Architecture:** Keep each correction at its canonical boundary: shell layout, shared unit presentation metrics, selected-unit refresh after combat, sprite source palette, and shared attack validation. Tests mirror those boundaries so future callers cannot bypass the behavior.

**Tech Stack:** TypeScript, Vitest, JSDOM, Canvas renderer, DOM sprite overlay.

---

### Task 1: Make game-shell utility controls non-overlapping

**Files:**
- Modify: `src/ui/game-shell.ts`
- Test: `tests/ui/game-shell.test.ts`

- [ ] Write a failing JSDOM test asserting the utility controls are children of a flex toolbar with a gap, rather than individually positioned controls with colliding right offsets.
- [ ] Run `bash scripts/run-with-mise.sh yarn test --run tests/ui/game-shell.test.ts`; expect the new assertion to fail.
- [ ] Create the toolbar and move all utility controls into it; preserve IDs, labels, callbacks, z-index, and placement below the HUD.
- [ ] Re-run the focused test and inspect the rendered styles.

### Task 2: Preserve unit size across render modes

**Files:**
- Modify: `src/renderer/sprite-overlay.ts` only if the test finds divergent sizing
- Test: `tests/renderer/sprite-overlay.test.ts`, `tests/renderer/render-loop-wrap.test.ts`

- [ ] Write failing coverage that creates static and moving unit entities with the same camera and verifies both wrappers use `getUnitLayoutMetrics(camera.hexSize).displaySize`.
- [ ] Run `bash scripts/run-with-mise.sh yarn test --run tests/renderer/sprite-overlay.test.ts tests/renderer/render-loop-wrap.test.ts`; expect a failing shared-size assertion.
- [ ] Apply the smallest shared-layout correction identified by the reproduction, without changing sprite source geometry.
- [ ] Re-run both renderer suites.

### Task 3: Refresh selected zone-of-control guidance after combat

**Files:**
- Modify: `src/main.ts`
- Test: a focused main/combat integration test matching the existing browser-runtime test harness

- [ ] Add a failing test that selects a unit whose panel has the warning, removes the last hostile ZOC source through combat, and verifies the still-open panel no longer contains `data-zone-of-control-warning` before delayed next-unit selection.
- [ ] Run its focused test; expect stale panel content.
- [ ] Refresh the surviving selected unit after applying combat state and update the test to prove it is current-state driven.
- [ ] Run the focused integration test.

### Task 4: Make archer ownership visually dominant

**Files:**
- Modify: `src/renderer/sprites/units.tsx`
- Regenerate: `src/renderer/sprites/v2/archer.svg.ts`
- Test: `tests/renderer/sprites/sprite-catalog.test.ts` or a focused sprite-source regression test

- [ ] Add a failing test that renders ArcherSprite with two faction palettes and verifies its primary clothing colors differ by palette.
- [ ] Run the focused sprite test; expect fixed green clothing to violate it.
- [ ] Route the archer tunic and cap through the faction palette and regenerate only the committed serialized artifact using the repository script.
- [ ] Re-run the focused test.

### Task 5: Block attacks with zero movement

**Files:**
- Modify: `src/systems/attack-targeting.ts`
- Test: `tests/systems/attack-targeting.test.ts`

- [ ] Replace the existing zero-movement ranged allowance with a failing test requiring `canUnitAttackTarget` and `getAttackTargets` to reject the attacker.
- [ ] Run `bash scripts/run-with-mise.sh yarn test --run tests/systems/attack-targeting.test.ts`; expect failure from the current legality path.
- [ ] Reject `movementPointsLeft <= 0` in shared attack validation and ensure the target enumeration inherits the rule.
- [ ] Re-run the focused system test.

### Task 6: Verify and review

**Files:**
- Verify all changed source files with `scripts/check-src-rule-violations.sh`.

- [ ] Run every mirrored focused test in one command.
- [ ] Run `bash scripts/run-with-mise.sh yarn build`.
- [ ] Run `bash scripts/run-with-mise.sh yarn test`.
- [ ] Inspect `git diff --check`, `git diff --stat origin/main...HEAD`, `git diff --stat`, and the full branch plus uncommitted diffs before reporting results.
